<%= render partial: "map" %>
<%= render partial: "filters" %>

<% (@events || []).each_value do |artist_concerts| %>
  <% artist_name = artist_concerts[0].artist if artist_concerts[0].respond_to?(:artist)%>
  <% artist = (RSpotify::Artist.search(artist_name) || [])[0] if (artist || NullArtist.new).name != artist_name%>
  <% Array(artist_concerts || []).each do |concerts| %>
    <% Array(concerts || []).each do |concert| %>
      <div class="container">
        <div class="row">
          <div class="col s2 m2 h2"></div>
          <div id="events" class="col s8 m8 l8">
            <div class="row">
              <div class="card sticky-action horizontal">
                <div class="card-image waves-effect waves-block waves-light">
                  <img class="activator" src="<%= artist.images.first['url'] rescue nil %>" />
                </div>
                <div class="card-stacked">
                  <div class="card-content">
                    <span class="card-title activator grey-text text-darken-4">
                      <p>Artist Name: <%= artist_name %></p>
                      <p>Venue: <%= concert.venue %></p>
                      <p>Location: <%= concert.city %> <%= concert.country['name'] %></p>
                      <p>Date: <%= concert.date %></p>

                      <% binding.pry %>
                      <a class="btn search" href="https://seatgeek.com/<%= artist_name %>-tickets?lat=<%= concert.coords['lat'] %>&lon=<%= concert.coords['long'] %>"  target="_blank">Buy your ticket! </a>
                      <a class="btn search" href="https://www.airbnb.com/s/<%=concert.city%>" target="_blank">Book your hotel!</a>
                    </span>
                  </div>
                </div>
                <div class="card-reveal">
                  <span class="card-title grey-text text-darken-4">Card Title<i class="material-icons right">close</i></span>
                  <p>Here is some more information about this product that is only revealed once clicked on.</p>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    <%end%>
  <%end%>
<%end%>
